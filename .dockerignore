# Keep the build context small and the image clean. LICENSE is intentionally
# NOT ignored — the Dockerfile copies it into the image.
.git
.gitignore
.env
.env.example
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.venv/
venv/
env/
tests/
docs/
.github/
README.md
Dockerfile
.dockerignore
docker-compose.yml
docker-compose.ghcr.yml
requirements-dev.txt
